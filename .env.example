PORT=3000
NODE_ENV=development

# Resend API (free, 100 emails/day)
# Sign up at https://resend.com → Dashboard → API Keys
RESEND_API_KEY=

# The verified sender address from Resend Dashboard → Domains
EMAIL_FROM=RobustOTP <cytrus@resend.dev>

# Vercel KV (optional — for Vercel deployment)
# Run: npx vercel env pull .env
# Or manually set in Vercel Dashboard → Storage → KV → Connect
# KV_URL=
