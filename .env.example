PORT=3000
NODE_ENV=development

# Neon PostgreSQL — get from https://neon.tech
DATABASE_URL=postgresql://demo_user:npg_0oP6zxeBSDlg@ep-morning-feather-azdogb8t-pooler.c-3.ap-southeast-1.aws.neon.tech/robust_demo?sslmode=require&channel_binding=require

# Brevo API — get from https://app.brevo.com → SMTP & API → API Keys
BREVO_API_KEY=

# Verified sender in Brevo Dashboard → Senders
EMAIL_FROM=

# Keep-Alive emails (comma-separated, to keep Brevo API active)
KEEP_ALIVE_EMAILS=

# Cron authorization token
CRON_SECRET=

# SMTP configuration for free direct delivery fallback (e.g., smtp.gmail.com)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
