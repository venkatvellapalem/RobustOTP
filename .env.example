PORT=3000
NODE_ENV=development

# Neon PostgreSQL (free serverless Postgres)
# Sign up at https://neon.tech → create project → copy connection string
DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Resend API (free, 100 emails/day)
# Sign up at https://resend.com → Dashboard → API Keys
RESEND_API_KEY=

# Verified sender address in Resend Dashboard → Domains
EMAIL_FROM=RobustOTP <cytrus@resend.dev>
